{
  "name": "ai-dnd",
  "version": "0.1.0",
  "private": true,
  "description": "A single-player AI-assisted Dungeons & Dragons game foundation",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
